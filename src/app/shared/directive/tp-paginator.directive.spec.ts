import { TpPaginatorDirective } from './tp-paginator.directive';

describe('TpPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TpPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
